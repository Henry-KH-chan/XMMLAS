name: xmmkas_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - tifffile
  - scipy
  - scikit-image
  - matplotlib
  - h5py
  # For training functionality, install TensorFlow:
  # For GPU support (requires appropriate CUDA/cuDNN installation), uncomment the next line:
  # - tensorflow-gpu=2.10.0
  # For CPU-only training (default):
  # - tensorflow=2.10.0
